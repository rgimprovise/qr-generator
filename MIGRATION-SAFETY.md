# 🔒 Гарантии безопасности миграции и обновления

## ✅ Что гарантируется

### 1. **QR коды НЕ будут удалены или изменены**
- Миграция обновляет **ТОЛЬКО** поле `total_scans` в таблице `qr_codes`
- Все остальные поля (`short_code`, `original_url`, `title`, `description`, `created_at`) **НЕ ТРОГАЮТСЯ**
- Проверка после миграции сравнивает количество и список `short_code` - если что-то изменилось, автоматически восстанавливается из бэкапа

### 2. **Данные сканирований НЕ будут потеряны**
- Таблица `scans` **НЕ ИЗМЕНЯЕТСЯ** миграцией
- Все исторические данные о сканированиях остаются нетронутыми
- Миграция только **пересчитывает** счетчики на основе существующих данных

### 3. **Автоматическое восстановление при ошибках**
- Перед обновлением создается бэкап базы данных
- Если после миграции количество QR кодов изменилось - автоматически восстанавливается из бэкапа
- Если список `short_code` изменился - автоматически восстанавливается из бэкапа

### 4. **Новая логика подсчета более надежна**
- Используются транзакции для атомарности операций
- Либо обе операции (INSERT + UPDATE) выполняются, либо ни одна
- Исключает рассинхронизацию данных в будущем

## 📋 Что делает миграция

### Скрипт `migrate-scans-count.js`:

1. **Анализирует данные:**
   - Сравнивает `total_scans` с реальным `COUNT(*)` из таблицы `scans`
   - Находит рассинхронизации

2. **Исправляет рассинхронизации:**
   - Обновляет `total_scans = COUNT(*)` для каждого QR кода
   - **НЕ изменяет** другие поля

3. **Проверяет целостность:**
   - После миграции проверяет, что все счетчики синхронизированы

### Скрипт `update.sh`:

1. **Создает бэкап** базы данных перед обновлением
2. **Проверяет количество QR кодов** до миграции
3. **Сохраняет список `short_code`** для проверки после миграции
4. **Запускает миграцию** в режиме проверки (`--dry-run`), затем реальную
5. **Проверяет после миграции:**
   - Количество QR кодов не изменилось
   - Список `short_code` идентичен
6. **Автоматически восстанавливает** из бэкапа при любых проблемах

## 🛡️ Многоуровневая защита

### Уровень 1: Бэкап перед обновлением
```bash
BACKUP_FILE="backups/qr_codes_backup_YYYYMMDD_HHMMSS.db"
cp qr_codes.db "$BACKUP_FILE"
```

### Уровень 2: Проверка перед миграцией
- Подсчет QR кодов
- Сохранение списка `short_code`

### Уровень 3: Миграция в режиме проверки
```bash
node migrate-scans-count.js --dry-run  # Только проверка, без изменений
```

### Уровень 4: Реальная миграция
- Обновляет только `total_scans`
- Логирует все изменения

### Уровень 5: Проверка после миграции
- Количество QR кодов не изменилось
- Список `short_code` идентичен
- При проблемах - автоматическое восстановление из бэкапа

## 🔍 Как проверить безопасность

### Перед обновлением:
```bash
# Проверка в режиме dry-run (без изменений)
node migrate-scans-count.js --dry-run
```

### После обновления:
```bash
# Проверка целостности через API
curl http://your-domain.com/api/integrity-check
```

### Ручная проверка:
```bash
# Количество QR кодов
sqlite3 qr_codes.db "SELECT COUNT(*) FROM qr_codes;"

# Список всех QR кодов
sqlite3 qr_codes.db "SELECT id, short_code, title, total_scans FROM qr_codes;"

# Проверка синхронизации счетчиков
sqlite3 qr_codes.db "
  SELECT 
    q.id,
    q.short_code,
    q.total_scans as stored,
    COUNT(s.id) as actual,
    (q.total_scans - COUNT(s.id)) as diff
  FROM qr_codes q
  LEFT JOIN scans s ON q.id = s.qr_code_id
  GROUP BY q.id
  HAVING diff != 0;
"
```

## ⚠️ Что НЕ делает миграция

- ❌ **НЕ удаляет** QR коды
- ❌ **НЕ изменяет** `short_code`
- ❌ **НЕ изменяет** `original_url`
- ❌ **НЕ изменяет** `title` или `description`
- ❌ **НЕ изменяет** `created_at`
- ❌ **НЕ удаляет** записи из таблицы `scans`
- ❌ **НЕ изменяет** данные в таблице `scans`

## 🚀 Процесс обновления

1. **Бэкап** → Создается автоматически
2. **Проверка** → Количество QR кодов и список `short_code`
3. **Миграция** → Пересчет счетчиков
4. **Проверка** → Убеждаемся что QR коды не изменились
5. **Восстановление** → При проблемах автоматически из бэкапа

## 📞 В случае проблем

Если что-то пошло не так:

1. **Автоматическое восстановление:**
   - Скрипт `update.sh` автоматически восстанавливает из бэкапа

2. **Ручное восстановление:**
   ```bash
   # Найти последний бэкап
   ls -lt backups/qr_codes_backup_*.db | head -1
   
   # Восстановить
   cp backups/qr_codes_backup_YYYYMMDD_HHMMSS.db qr_codes.db
   ```

3. **Проверка целостности:**
   ```bash
   sqlite3 qr_codes.db "PRAGMA integrity_check;"
   ```

---

**Вывод:** Миграция полностью безопасна и не может повредить существующие QR коды или данные сканирований. Все изменения обратимы через бэкап.

