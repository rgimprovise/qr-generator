# 🔄 Команды для обновления на VPS (PostgreSQL версия)

## ⚠️ ВАЖНО: Безопасность данных

**Гарантии:**
- ✅ Миграция обновляет **ТОЛЬКО** поле `total_scans`
- ✅ Все остальные данные (QR коды, сканирования) **НЕ ИЗМЕНЯЮТСЯ**
- ✅ Автоматический бэкап перед обновлением
- ✅ Проверка целостности после миграции

---

## 📋 Требования

Перед обновлением убедитесь, что установлены:
- `node` и `npm`
- `pm2`
- `psql` (PostgreSQL клиент) - для проверок
- `pg_dump` (PostgreSQL утилита) - для бэкапов

---

## 🚀 Стандартное обновление (рекомендуется)

```bash
# 1. Подключитесь к VPS
ssh your-user@your-server-ip

# 2. Перейдите в директорию проекта
cd /var/www/qr-generator
# или
cd ~/qr-generator
# или
cd /path/to/your/qr-generator

# 3. Убедитесь что файл .env настроен с DATABASE_URL
# Пример .env:
# DATABASE_URL=postgresql://user:password@host:port/database

# 4. Запустите скрипт обновления для PostgreSQL
./update-postgres.sh
```

Скрипт автоматически:
- ✅ Создаст бэкап базы данных через `pg_dump`
- ✅ Проверит количество QR кодов перед миграцией
- ✅ Получит изменения из GitHub
- ✅ Установит новые зависимости
- ✅ Запустит миграцию для пересчета счетчиков
- ✅ Проверит, что QR коды не изменились
- ✅ Перезапустит приложение
- ✅ Покажет статус

---

## 📋 Пошаговое обновление (ручное)

Если хотите больше контроля:

```bash
# 1. Подключитесь к VPS
ssh your-user@your-server-ip

# 2. Перейдите в директорию проекта
cd /var/www/qr-generator

# 3. Загрузите переменные окружения
export $(cat .env | grep -v '^#' | xargs)

# 4. Создайте бэкап базы данных (ОБЯЗАТЕЛЬНО!)
mkdir -p backups
pg_dump "$DATABASE_URL" > backups/qr_codes_backup_$(date +%Y%m%d_%H%M%S).sql

# Или если используете отдельные переменные:
# PGPASSWORD="$DB_PASSWORD" pg_dump -h "$DB_HOST" -p "${DB_PORT:-5432}" -U "$DB_USER" -d "$DB_NAME" > backups/backup.sql

# 5. Проверьте текущее состояние
psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM qr_codes;"
psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM scans;"

# 6. Сохраните список short_code для проверки
psql "$DATABASE_URL" -t -c "SELECT short_code FROM qr_codes ORDER BY id;" > /tmp/short_codes_before.txt

# 7. Получите изменения из GitHub
git fetch origin
git pull origin main

# 8. Установите зависимости
npm install --production

# 9. Запустите миграцию для пересчета счетчиков
# Сначала проверка (без изменений):
node migrate-scans-count-postgres.js --dry-run

# Затем реальная миграция:
node migrate-scans-count-postgres.js

# 10. Проверьте что QR коды не изменились
QR_AFTER=$(psql "$DATABASE_URL" -t -c "SELECT COUNT(*) FROM qr_codes;" | xargs)
SHORT_CODES_AFTER=$(psql "$DATABASE_URL" -t -c "SELECT short_code FROM qr_codes ORDER BY id;" | xargs)

# Сравните с сохраненными значениями
if [ "$QR_AFTER" != "$QR_BEFORE" ]; then
    echo "ОШИБКА: Количество QR кодов изменилось!"
    # Восстановите из бэкапа
    psql "$DATABASE_URL" < backups/qr_codes_backup_YYYYMMDD_HHMMSS.sql
fi

# 11. Перезапустите приложение
pm2 restart qr-generator

# 12. Проверьте статус
pm2 status qr-generator
pm2 logs qr-generator --lines 20
```

---

## 🔍 Проверка миграции (без изменений)

Если хотите сначала проверить, что будет исправлено:

```bash
# 1. Подключитесь к VPS
ssh your-user@your-server-ip

# 2. Перейдите в директорию проекта
cd /var/www/qr-generator

# 3. Загрузите переменные окружения
export $(cat .env | grep -v '^#' | xargs)

# 4. Проверка в режиме dry-run (без изменений)
node migrate-scans-count-postgres.js --dry-run

# 5. Если все ок, запустите реальную миграцию
node migrate-scans-count-postgres.js
```

---

## 🔧 Настройка переменных окружения

Убедитесь, что в файле `.env` настроена строка подключения:

```bash
# Вариант 1: Полная строка подключения
DATABASE_URL=postgresql://user:password@host:port/database

# Вариант 2: Отдельные переменные
DB_HOST=localhost
DB_PORT=5432
DB_NAME=qr_generator
DB_USER=postgres
DB_PASSWORD=your_password
```

---

## 💾 Восстановление из бэкапа

Если что-то пошло не так:

```bash
# 1. Найдите последний бэкап
ls -lt backups/*.sql | head -1

# 2. Восстановите базу данных
psql "$DATABASE_URL" < backups/qr_codes_backup_YYYYMMDD_HHMMSS.sql

# Или если используете отдельные переменные:
# PGPASSWORD="$DB_PASSWORD" psql -h "$DB_HOST" -p "${DB_PORT:-5432}" -U "$DB_USER" -d "$DB_NAME" < backups/backup.sql

# 3. Перезапустите приложение
pm2 restart qr-generator
```

---

## ✅ Проверка целостности после обновления

```bash
# Через API (если endpoint доступен)
curl http://your-domain.com/api/integrity-check

# Или вручную через psql
psql "$DATABASE_URL" -c "
  SELECT 
    q.short_code,
    q.total_scans as stored,
    COUNT(s.id) as actual,
    (q.total_scans - COUNT(s.id)) as diff
  FROM qr_codes q
  LEFT JOIN scans s ON q.id = s.qr_code_id
  GROUP BY q.id, q.short_code, q.total_scans
  HAVING q.total_scans != COUNT(s.id);
"
```

Если запрос вернул пустой результат - все счетчики синхронизированы! ✅

---

## 📦 Установка зависимостей для миграции

Если скрипт миграции требует дополнительные пакеты:

```bash
# Установите pg и dotenv если их нет
npm install pg dotenv --save
```

---

## ⚠️ Важные моменты

1. **Путь к проекту** может отличаться. Уточните:
   ```bash
   # Найти где находится проект
   pm2 list
   pm2 info qr-generator
   ```

2. **Если скрипт не исполняемый:**
   ```bash
   chmod +x update-postgres.sh
   chmod +x migrate-scans-count-postgres.js
   ./update-postgres.sh
   ```

3. **Если нет доступа к psql/pg_dump:**
   - Установите PostgreSQL клиент: `sudo apt install postgresql-client`
   - Или используйте ручное обновление без этих проверок

---

## 🎯 Что произойдет при обновлении

1. ✅ Создастся бэкап базы данных (через `pg_dump`)
2. ✅ Проверится количество QR кодов (должно быть 5)
3. ✅ Сохранится список всех `short_code`
4. ✅ Запустится миграция для пересчета счетчиков
5. ✅ Проверится, что QR коды не изменились
6. ✅ При проблемах - инструкции по восстановлению из бэкапа

---

**Готово!** Используйте `./update-postgres.sh` для безопасного обновления.

