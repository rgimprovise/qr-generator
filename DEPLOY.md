# 🚀 Инструкция по развертыванию на VPS сервере

Подробное руководство по установке и настройке QR Generator на VPS сервере.

---

## 📋 Требования

- VPS сервер с Ubuntu 20.04/22.04 (или другой Linux дистрибутив)
- Минимум 1GB RAM
- Доступ по SSH
- Домен (опционально, но рекомендуется)

---

## 🔧 Шаг 1: Подготовка сервера

### 1.1 Подключение к серверу

```bash
ssh root@your-server-ip
# или
ssh your-user@your-server-ip
```

### 1.2 Обновление системы

```bash
sudo apt update && sudo apt upgrade -y
```

### 1.3 Установка Node.js

```bash
# Установка Node.js 18.x (LTS)
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# Проверка установки
node -v
npm -v
```

### 1.4 Установка дополнительных инструментов

```bash
# Git для клонирования репозитория
sudo apt install -y git

# Build tools для нативных модулей
sudo apt install -y build-essential
```

---

## 📦 Шаг 2: Загрузка проекта на сервер

### Вариант A: Через Git

```bash
# Создайте репозиторий на GitHub/GitLab и загрузите туда проект
cd /var/www
sudo git clone https://github.com/your-username/qr-generator.git
cd qr-generator
```

### Вариант B: Через SCP (прямая загрузка)

На вашем локальном компьютере:

```bash
# Упакуйте проект (исключая node_modules)
cd "/Users/rostislavgolivetc/Downloads/QR generator"
tar -czf qr-generator.tar.gz \
  --exclude='node_modules' \
  --exclude='qr_codes.db' \
  --exclude='.git' \
  .

# Загрузите на сервер
scp qr-generator.tar.gz your-user@your-server-ip:/tmp/

# На сервере распакуйте
ssh your-user@your-server-ip
sudo mkdir -p /var/www/qr-generator
sudo tar -xzf /tmp/qr-generator.tar.gz -C /var/www/qr-generator
cd /var/www/qr-generator
```

### Вариант C: Через rsync (рекомендуется для обновлений)

```bash
# На локальном компьютере
rsync -avz --exclude='node_modules' --exclude='qr_codes.db' \
  "/Users/rostislavgolivetc/Downloads/QR generator/" \
  your-user@your-server-ip:/var/www/qr-generator/
```

---

## ⚙️ Шаг 3: Настройка приложения

### 3.1 Установка зависимостей

```bash
cd /var/www/qr-generator
npm install --production
```

### 3.2 Настройка переменных окружения

```bash
# Создайте .env файл
cp .env.example .env
nano .env
```

Отредактируйте файл:

```env
PORT=3000
NODE_ENV=production
DOMAIN=yourdomain.com
```

Сохраните: `Ctrl+X`, затем `Y`, затем `Enter`

### 3.3 Права доступа

```bash
# Если нужно, настройте владельца
sudo chown -R $USER:$USER /var/www/qr-generator

# Права на исполнение для скрипта деплоя
chmod +x deploy.sh
```

---

## 🔄 Шаг 4: Установка PM2 (Process Manager)

```bash
# Установка PM2 глобально
sudo npm install -g pm2

# Запуск приложения
pm2 start ecosystem.config.js

# Сохранение конфигурации
pm2 save

# Настройка автозапуска при перезагрузке сервера
pm2 startup
# Выполните команду, которую покажет PM2
```

### Полезные команды PM2:

```bash
pm2 status              # Статус приложения
pm2 logs qr-generator   # Просмотр логов
pm2 restart qr-generator # Перезапуск
pm2 stop qr-generator    # Остановка
pm2 delete qr-generator  # Удаление из PM2
pm2 monit               # Мониторинг в реальном времени
```

---

## 🌐 Шаг 5: Настройка Nginx (рекомендуется)

### 5.1 Установка Nginx

```bash
sudo apt install -y nginx
```

### 5.2 Настройка конфигурации

```bash
# Создайте конфигурационный файл
sudo nano /etc/nginx/sites-available/qr-generator
```

Скопируйте содержимое файла `nginx.conf` из проекта и замените:
- `yourdomain.com` на ваш реальный домен
- Или используйте IP адрес вместо домена

```bash
# Создайте симлинк
sudo ln -s /etc/nginx/sites-available/qr-generator /etc/nginx/sites-enabled/

# Удалите дефолтную конфигурацию
sudo rm /etc/nginx/sites-enabled/default

# Проверьте конфигурацию
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

### 5.3 Настройка Firewall

```bash
# Разрешите HTTP и HTTPS трафик
sudo ufw allow 'Nginx Full'
sudo ufw allow ssh
sudo ufw enable
```

---

## 🔒 Шаг 6: Установка SSL сертификата (опционально, но рекомендуется)

### 6.1 Установка Certbot

```bash
sudo apt install -y certbot python3-certbot-nginx
```

### 6.2 Получение SSL сертификата

```bash
# Замените на ваш домен
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com
```

Следуйте инструкциям Certbot. Он автоматически настроит Nginx для HTTPS.

### 6.3 Автоматическое обновление сертификата

```bash
# Certbot автоматически создает cron задачу
# Проверить можно так:
sudo certbot renew --dry-run
```

---

## 🚀 Шаг 7: Автоматический деплой (скрипт)

### Использование встроенного скрипта:

```bash
cd /var/www/qr-generator
./deploy.sh
```

Этот скрипт автоматически:
- Проверяет зависимости
- Устанавливает пакеты
- Перезапускает приложение через PM2
- Настраивает автозапуск

---

## 📊 Шаг 8: Проверка работы

### 8.1 Проверка статуса

```bash
# Статус PM2
pm2 status

# Статус Nginx
sudo systemctl status nginx

# Логи приложения
pm2 logs qr-generator --lines 50
```

### 8.2 Тестирование

Откройте в браузере:
- `http://your-server-ip` (если без домена)
- `http://yourdomain.com` (если с доменом)
- `https://yourdomain.com` (если установлен SSL)

---

## 🔄 Обновление приложения

### Метод 1: Автоматический (через скрипт)

```bash
cd /var/www/qr-generator
git pull origin main  # если используете Git
./deploy.sh
```

### Метод 2: Ручной

```bash
cd /var/www/qr-generator

# Обновите код (через Git или загрузите новые файлы)
git pull

# Установите зависимости
npm install --production

# Перезапустите приложение
pm2 restart qr-generator
```

### Метод 3: С локального компьютера (rsync)

```bash
# На локальном компьютере
rsync -avz --exclude='node_modules' --exclude='qr_codes.db' \
  "/Users/rostislavgolivetc/Downloads/QR generator/" \
  your-user@your-server-ip:/var/www/qr-generator/

# Затем на сервере
ssh your-user@your-server-ip
cd /var/www/qr-generator
npm install --production
pm2 restart qr-generator
```

---

## 🔧 Решение проблем

### Проблема: Приложение не запускается

```bash
# Проверьте логи
pm2 logs qr-generator

# Проверьте порт
sudo netstat -tulpn | grep 3000

# Убейте процесс, если порт занят
sudo kill -9 $(sudo lsof -t -i:3000)
```

### Проблема: Nginx показывает 502 Bad Gateway

```bash
# Проверьте, что приложение запущено
pm2 status

# Проверьте логи Nginx
sudo tail -f /var/log/nginx/error.log

# Перезапустите приложение
pm2 restart qr-generator
```

### Проблема: База данных не создается

```bash
# Проверьте права доступа
ls -la /var/www/qr-generator

# Создайте директорию для БД вручную
mkdir -p /var/www/qr-generator
chmod 755 /var/www/qr-generator
```

### Проблема: Недостаточно памяти

```bash
# Добавьте swap
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

---

## 📈 Мониторинг и обслуживание

### Логи

```bash
# Логи приложения
pm2 logs qr-generator

# Логи Nginx
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# Системные логи
sudo journalctl -u nginx -f
```

### Резервное копирование базы данных

```bash
# Создайте скрипт для бэкапа
nano /var/www/qr-generator/backup.sh
```

Содержимое скрипта:

```bash
#!/bin/bash
BACKUP_DIR="/var/backups/qr-generator"
mkdir -p $BACKUP_DIR
DATE=$(date +%Y%m%d_%H%M%S)
cp /var/www/qr-generator/qr_codes.db $BACKUP_DIR/qr_codes_$DATE.db
# Удаление старых бэкапов (старше 30 дней)
find $BACKUP_DIR -name "qr_codes_*.db" -mtime +30 -delete
```

```bash
# Сделайте скрипт исполняемым
chmod +x /var/www/qr-generator/backup.sh

# Добавьте в cron (ежедневно в 2:00)
crontab -e
# Добавьте строку:
0 2 * * * /var/www/qr-generator/backup.sh
```

---

## 🔐 Безопасность

### Рекомендации:

1. **Используйте SSH ключи** вместо паролей
2. **Настройте firewall** (ufw)
3. **Установите fail2ban** для защиты от брутфорса
4. **Регулярно обновляйте систему** и зависимости
5. **Используйте HTTPS** (SSL сертификат)
6. **Ограничьте доступ к базе данных**

```bash
# Установка fail2ban
sudo apt install -y fail2ban
sudo systemctl enable fail2ban
sudo systemctl start fail2ban
```

---

## ✅ Чек-лист деплоя

- [ ] Сервер настроен и обновлен
- [ ] Node.js установлен
- [ ] Проект загружен на сервер
- [ ] Зависимости установлены
- [ ] .env файл настроен
- [ ] PM2 установлен и настроен автозапуск
- [ ] Nginx установлен и настроен
- [ ] Firewall настроен
- [ ] SSL сертификат установлен (опционально)
- [ ] Приложение доступно через браузер
- [ ] Резервное копирование настроено

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `pm2 logs`
2. Проверьте статус: `pm2 status`
3. Проверьте конфигурацию Nginx: `sudo nginx -t`
4. Проверьте firewall: `sudo ufw status`

---

## 🎉 Готово!

Ваше приложение успешно развернуто и готово к использованию!

**Доступ к приложению:**
- HTTP: `http://yourdomain.com`
- HTTPS: `https://yourdomain.com`

**Полезные ресурсы:**
- PM2 документация: https://pm2.keymetrics.io/
- Nginx документация: https://nginx.org/en/docs/
- Let's Encrypt: https://letsencrypt.org/

---

Создано с ❤️ в 2025 году

